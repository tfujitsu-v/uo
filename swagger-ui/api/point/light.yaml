openapi: "3.0.0"
info:
  version: 1.0.0
  title: mixway API Documents
servers:
  - url: https://mixway.ekispert.jp
security:
  - ApiKeyAuth: []
paths:
  /v1/json/point/light:
    get:
      tags:
        - 地点の簡易情報
      description: |-
        ### 概要
        パラメータの名前からポート情報、デマンド情報、駅すぱぁとWEBサービスから検索し、簡易情報(最大30件)を返します。<br>


        ### リクエストパラメータ
        <table class="comparison">
        <tr><th>パラメータ名</th><th>EWS</th><th>mixway</th><th>備考</th></tr>
        <tr><td>key</td><td>-</td><td>○</td><td></td></tr>
        <tr><td>name</td><td>-</td><td>○</td><td>検索に使用します。</td></tr>
        </table>
        <div class="about-comparison-table">
          ○＝パラメータあり　☓＝パラメータなし　両方○でも機能差異がある場合は備考欄および後述のParameters節に記載。
        </div>

        ### レスポンス
        本APIのレスポンス構造は、Mixway API独自のものになります。
      parameters:
        - name: name
          description: |-
            **検索文字列**。必須。
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: 正常レスポンスの場合
components:
  securitySchemes:
    ApiKeyAuth:        # arbitrary name for the security scheme
      type: apiKey
      in: query       # can be "header", "query" or "cookie"
      name: key  # name of the header, query parameter or cookie
